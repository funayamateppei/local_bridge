# TODO List

---

## Local-First アーキテクチャ評価サマリー

### 評価スコア: 85/100

| 項目 | スコア | 備考 |
|------|--------|------|
| オフライン動作 | 10/10 | IndexedDB + OPFS で完全オフライン対応 |
| クライアントサイドID | 10/10 | UUID v4 をクライアント生成、サーバー依存なし |
| ローカルDB設計 | 10/10 | Dexie.js + OPFS の適切な分離 |
| 同期戦略 | 8/10 | 手動同期 + LWW は適切、差分同期が未実装 |
| 競合解決 | 9/10 | Append-Only で競合回避、シンプルで十分 |
| ファイル管理 | 10/10 | OPFS 活用で IndexedDB 50MB 制限を回避 |
| アーキテクチャ | 10/10 | Clean Architecture + DDD の正しい適用 |
| エラーハンドリング | 7/10 | リトライ機構・エクスポネンシャルバックオフ未実装 |
| 認証・セキュリティ | 6/10 | TODO 項目多数 |
| 本番運用準備 | 5/10 | S3 連携・CORS 設定など未実装 |

### ベストプラクティス準拠状況

**正しく実装されている点:**
- クライアントサイドID生成（UUID v4）
- IndexedDB を Source of Truth として使用
- OPFS による大容量ファイル管理
- 手動同期による予測可能な動作
- Append-Only 結果によるコンフリクトフリー設計
- Clean Architecture の層分離

**設計判断の妥当性:**
- CRDT を使用しない → 正しい判断（このユースケースには過剰）
- 手動同期 → 正しい判断（現場検査のワークフローに適合）
- Zustand + Dexie.js → 正しい選択（Local-First に適した組み合わせ）

---

## 認証関連

### トークン管理の改善

- [ ] **トークンの有効期限チェック**: 現在、期限切れのトークンでもログイン済みとみなされる
  - JWT のペイロードから`exp`を読み取り、期限切れの場合はログアウト処理を行う
  - 実装場所: `AuthProvider`の初期化時、またはカスタムフック
- [ ] **トークンの検証**: 現在、改ざんされたトークンでもアクセス可能
  - オフライン時は基本的に信頼するが、ネットワーク回復時にバックエンドで検証
  - 検証失敗時はログアウトしてログインページにリダイレクト
  - 実装場所: 同期処理（Sync Layer）

### セキュリティ

- [ ] **リフレッシュトークンの実装**: 現在はアクセストークンのみ（有効期限 30 日）
  - 本番環境ではリフレッシュトークンを実装し、アクセストークンの有効期限を短くする
  - デモアプリのため現在は保留

## 同期機能の改善

### 差分同期の実装

- [ ] **マスターデータの差分取得**: 現在は毎回全件取得
  - `GET /api/master/areas?since={timestamp}` 形式で変更分のみ取得
  - 最後の同期タイムスタンプをローカルに保存
  - 実装場所: `SyncService.ts`

### エラーハンドリングの強化

- [ ] **リトライ機構の実装**: 現在、同期失敗時のリトライがない
  - エクスポネンシャルバックオフ（1s → 2s → 4s → 8s → 最大10s）
  - 最大リトライ回数: 3回
  - 実装場所: `ApiClient.ts`

- [ ] **同期キューの永続化**: 現在、同期状態はメモリのみ
  - IndexedDB に `syncQueue` テーブルを追加
  - アプリ再起動後も未同期データを保持
  - スキーマ例:
    ```typescript
    interface SyncQueueItem {
      id: string;
      type: 'result' | 'comment' | 'evidence';
      payload: any;
      retryCount: number;
      createdAt: number;
    }
    ```

- [ ] **部分同期失敗の詳細レポート**: 現在、失敗時の情報が不足
  - どのデータタイプが失敗したか表示
  - 失敗理由（ネットワーク、サーバーエラー、認証など）を区別
  - UI に詳細エラーを表示

### 同期進捗の可視化

- [ ] **大量データ同期時の進捗表示**
  - Evidence アップロード時: 「X / Y 件アップロード中」
  - 全体進捗バーの表示
  - 実装場所: `useSyncStore.ts` に `progress` state を追加

## インフラストラクチャ

### S3 連携

- [ ] **Evidence ファイルの S3 アップロード実装**
  - Presigned URL 取得 API の実装（バックエンド）
  - OPFS → S3 へのアップロード処理（フロントエンド）
  - アップロード完了後の OPFS クリーンアップ
  - 参照: `SyncService.ts` 内の TODO コメント

### 本番環境設定

- [ ] **CORS 設定の修正**: 現在 `@CrossOrigin(origins = ["*"])`
  - 本番環境のドメインのみ許可
  - 実装場所: `InspectionController.kt`

- [ ] **環境変数の整理**
  - フロントエンド: `.env.production` の設定
  - バックエンド: `application-prod.yml` の作成

## その他

- [ ] バックエンドの Hibernate Dialect 設定エラーの修正

---

## 将来の拡張（優先度: 低）

以下は現在のスコープ外だが、将来的に検討可能な改善点:

- [ ] **バージョンベクター**: 複数デバイス対応が必要な場合
- [ ] **End-to-End 暗号化**: 機密性の高い検査データ向け
- [ ] **Service Worker 更新戦略の改善**: キャッシュ無効化の制御
- [ ] **オフライン分析**: 同期履歴・転送量のロギング
